const chalk = require('chalk');
const { cyan, yellow, blue, green, magenta, gray, red } = chalk;

// app.on('pre-build', async () => {
//   console.log(green(app.config.site_name));
// });

// handle content
// app.on('build.md', text => {
// });